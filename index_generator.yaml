index_generator:
  exclude:
    - '**.g.dart'
    - '{_,**/_}*.dart'
  # Define the paths of the folders in which to generate the index files
  indexes:
    - path: lib/core
    - path: lib/features
      include:
        - '**/widget'
        - '**/ui'
      name: features